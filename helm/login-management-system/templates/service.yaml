apiVersion: v1
kind: Service
metadata:
  name: {{ include "login-management-system.fullname" . }}
  labels:
    {{- include "login-management-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "login-management-system.selectorLabels" . | nindent 4 }}
